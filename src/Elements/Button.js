import styled from 'styled-components';

const Button = styled.button`
    padding: .4em;
    border: 2px solid;
    font-weight: bold;
    border-radius: 4px;
    font-size: 1rem;
    font-family: 'Rubik';
    cursor: pointer;
`

export default Button;